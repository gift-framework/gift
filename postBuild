#!/bin/bash

# GIFT Framework v2 Post-Build Script for Binder
echo "🚀 GIFT Framework v2 Binder Setup"

# Enable Jupyter widgets
jupyter nbextension enable --py --sys-prefix ipywidgets

# Install additional tools if needed
pip install --no-deps gmpy2 || echo "gmpy2 installation failed, continuing..."

# Set up environment
export GIFT_VERSION=v2.0.0
export PYTHONPATH="${PYTHONPATH}:."

# Create symbolic links for easy access
ln -sf gift_main_v2.md README_main.md || echo "Could not create symlink"
ln -sf gift_technical_v2.md README_technical.md || echo "Could not create symlink"

echo "✅ GIFT Framework v2.0.0 Binder environment ready!"
echo "📖 Main paper: gift_main_v2.md"
echo "🔬 Technical supplement: gift_technical_v2.md"
echo "💻 Interactive notebook: gift_v2_notebook.ipynb"
